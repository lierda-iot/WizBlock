# net_mgmt Prebuilt Package

This package contains the prebuilt `net_mgmt` static library and its public headers.

## Build Metadata

- Target: esp32s3
- ESP-IDF version: ESP-IDF v5.5.3-dirty
- ESP-IDF version tag: idf5_5_3
- Export time (UTC): 2026-04-16T08:30:40Z

## Notes

- The consuming project must use a compatible target, ESP-IDF version, and required component configuration.
- Rebuild and re-export the library if the consuming environment uses a different ESP-IDF major or minor version.
- Component link dependencies are declared in `CMakeLists.txt`.
- Machine-readable metadata is stored in `package_info.txt`.
