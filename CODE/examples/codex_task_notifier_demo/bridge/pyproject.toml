[project]
name = "codex-task-bridge"
version = "0.1.0"
description = "Read-only Codex multi-task status bridge for codex_task_notifier_demo."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
codex-task-bridge = "codex_task_bridge.__main__:main"

[tool.pyright]
pythonVersion = "3.11"
