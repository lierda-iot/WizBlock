# Public Example Notes

[中文](README.md) | [English](README.en.md)

The machine-readable source is [`CODE/examples/examples.yml`](../../CODE/examples/examples.yml), with the human index at [`CODE/examples/README.en.md`](../../CODE/examples/README.en.md).

Only `publish` and `publish-with-limitations` directories enter the candidate. `hold` stays private, `archive` receives central documentation only, and `unavailable` never becomes a fake empty project. An Example README is a build/use entry, not an active requirements, design, memory, or TODO baseline.

