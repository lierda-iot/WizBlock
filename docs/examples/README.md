# Example 公开说明

[中文](README.md) | [English](README.en.md)

当前机器可读事实源为 [`CODE/examples/examples.yml`](../../CODE/examples/examples.yml)，可读索引为 [`CODE/examples/README.md`](../../CODE/examples/README.md)。

公开候选只复制 `publish` 与 `publish-with-limitations` 目录。`hold` 条目继续在私有事实源中开发/审计；`archive` 只保留中央说明；`unavailable` 不创建空目录伪装存在。

Example 目录中的 README 是构建和使用入口，不是活跃需求、设计、项目记忆或TODO事实源。内部状态、Round日志和设备证据只提炼成当前限制，不整份复制。

